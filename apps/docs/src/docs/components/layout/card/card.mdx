import { cardDoc as doc } from './data';
import { OverviewDemo, ImportSection, PropsSection } from '../../../../components/mdx/ComponentDocSections';
import { ComponentPlayground } from '../../../../components/content/ComponentPlayground';

export const meta = {
  name: doc.name,
  category: doc.category,
  description: doc.description,
};

<section id="section-overview">
## Overview

<OverviewDemo example={doc.example} />

A wrapper that visually separates content sections. Available in default (elevated) and outlined (bordered) variants.
</section>

<ImportSection name={doc.name} />

<section id="section-usage">
## Usage

### Default variant

The default card has a subtle background and shadow, giving it a slightly elevated look that works well for dashboard widgets and content panels.

<ComponentPlayground code={doc.usageExamples[0].code} />

### Outlined variant

The outlined variant uses a border instead of a shadow for a flatter appearance. It works well when you want clear boundaries without depth.

<ComponentPlayground code={doc.usageExamples[1].code} />
</section>

<PropsSection props={doc.props} />
