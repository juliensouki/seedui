import { buttonDoc as doc } from './data';
import { OverviewDemo, ImportSection, PropsSection } from '../../../../components/mdx/ComponentDocSections';
import { ComponentPlayground } from '../../../../components/content/ComponentPlayground';

export const meta = {
  name: doc.name,
  category: doc.category,
  description: doc.description,
};

<section id="section-overview">
## Overview

<OverviewDemo example={doc.example} />

Buttons let people take actions like submitting a form, confirming a choice, or starting a new flow. They come in filled and transparent variants across primary, neutral, and error colors.

</section>

<ImportSection name={doc.name} />

<section id="section-usage">
## Usage

### Colors

Use the color prop to match the button to its intent. Primary for main actions, neutral for general-purpose, and error for destructive operations.

<ComponentPlayground code={doc.usageExamples[0].code} layout={doc.usageExamples[0].layout} />

You can also pass any hex color and seedui will automatically generate the full shade scale for hover, focus, and active states.

<ComponentPlayground code={doc.usageExamples[1].code} layout={doc.usageExamples[1].layout} />

### Variants

Filled buttons draw the most attention and are best for primary actions. Transparent buttons work well for secondary or less prominent actions.

<ComponentPlayground code={doc.usageExamples[2].code} layout={doc.usageExamples[2].layout} />

### Sizes

Three sizes are available. Use small for dense UIs or inline actions, medium as the default, and large for prominent call-to-action buttons.

<ComponentPlayground code={doc.usageExamples[3].code} layout={doc.usageExamples[3].layout} />

### Loading state

Set <code>isLoading</code> to show a spinner and disable interaction. Useful while waiting for an async operation like a form submission.

<ComponentPlayground code={doc.usageExamples[4].code} layout={doc.usageExamples[4].layout} />

### Disabled

Disabled buttons are visually muted and ignore clicks. Use them to indicate that an action is unavailable.

<ComponentPlayground code={doc.usageExamples[5].code} layout={doc.usageExamples[5].layout} />

</section>

<PropsSection props={doc.props} />
