import { avatarStackDoc as doc } from './data';
import { OverviewDemo, ImportSection, AnatomySection, PropsSection } from '../../../../components/mdx/ComponentDocSections';
import { ComponentPlayground } from '../../../../components/content/ComponentPlayground';

export const meta = {
  name: doc.name,
  category: doc.category,
  description: doc.description,
};

<section id="section-overview">
## Overview

<OverviewDemo example={doc.example} />

Renders avatars in an overlapping row. Use max to limit visible avatars — overflow shows as a "+N" badge.
</section>

<ImportSection name={doc.name} />

<section id="section-usage">
## Usage

### Basic stack

Avatars overlap naturally. Each avatar receives a border to maintain visual separation.

<ComponentPlayground code={doc.usageExamples[0].code} layout={doc.usageExamples[0].layout} />

### With overflow

Set max to limit visible avatars. A "+N" indicator shows how many are hidden.

<ComponentPlayground code={doc.usageExamples[1].code} layout={doc.usageExamples[1].layout} />

### Sizes

The size prop controls all avatars in the stack uniformly.

<ComponentPlayground code={doc.usageExamples[2].code} layout={doc.usageExamples[2].layout} />

### Direction

Controls which avatar appears on top. "left" (default) puts the first avatar on top, "right" puts the last on top.

<ComponentPlayground code={doc.usageExamples[3].code} layout={doc.usageExamples[3].layout} />
</section>

<AnatomySection name={doc.name} anatomy={doc.anatomy} />

<PropsSection props={doc.props} />
