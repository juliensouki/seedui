import { inputDoc as doc } from './data';
import { OverviewDemo, ImportSection, AnatomySection, PropsSection } from '../../../../components/mdx/ComponentParts';
import { ComponentPlayground } from '../../../../components/ComponentPlayground';

export const meta = {
  name: doc.name,
  category: doc.category,
  description: doc.description,
};

<section id="section-overview">
## Overview

<OverviewDemo example={doc.example} />

A single-line text field with support for labels, icons, and validation. For multi-line content, use Textarea instead.
</section>

<ImportSection name={doc.name} />

<section id="section-usage">
## Usage

### Basic input

A controlled text input with a label and placeholder. This is the most common usage pattern.

<ComponentPlayground code={doc.usageExamples[0].code} />

### Password input

Set type to "password" to mask the input. The browser handles toggling visibility natively.

<ComponentPlayground code={doc.usageExamples[1].code} />

### With validation

Pass a validation function to provide real-time visual feedback. The function receives the current value and should return true if valid.

<ComponentPlayground code={doc.usageExamples[2].code} />

### Disabled

A disabled input is visually muted and does not accept user interaction.

<ComponentPlayground code={doc.usageExamples[3].code} />
</section>

<AnatomySection name={doc.name} anatomy={doc.anatomy} />

<PropsSection props={doc.props} />
