import { Button } from '@seedui-react/seedui';
import { ComponentPlayground } from '../../../components/content/ComponentPlayground';
import { Callout } from '../../../components/content/Callout';

export const meta = {
  title: 'Quick Start',
  description: 'Render your first seedui component in under a minute.',
};

<section id="section-minimal-example">
## A minimal example

After installing and wrapping your app in `ThemeProvider`, you can use any seedui component:

```tsx
import { ThemeProvider, Button } from '@seedui-react/seedui';

function App() {
  return (
    <ThemeProvider mode="light">
      <Button onClick={() => alert('Hello World!')}>Click me</Button>
    </ThemeProvider>
  );
}
```

Here's what the example above looks like when rendered:

<ComponentPlayground preview={<Button onClick={() => alert('Hello World!')}>Click me</Button>} />
</section>

<section id="section-styled">
## Using styled-components

<Callout color="warning">
  Do not import directly from <code>styled-components</code>.
</Callout>

The library re-exports styled-components API through `@seedui-react/seedui/sc`. Use this import for everything you would normally import from styled-components:

```tsx
import styled, { css, keyframes } from '@seedui-react/seedui/sc';

const Container = styled.div(({ theme }) => ({
  padding: theme.spacing(3),
  backgroundColor: theme.colors.primary[100],
  borderRadius: theme.borderRadius(4),
}));
```

This ensures you share the same styled-components instance as seedui, which is required for theming to work correctly.

</section>

<section id="section-use-theme">
## Using the useTheme hook

Access theme values directly in your components with `useTheme`:

```tsx
import { useTheme } from '@seedui-react/seedui';

function MyComponent() {
  const theme = useTheme();
  return <div style={{ color: theme.colors.primary[600] }}>Themed content</div>;
}
```

</section>
